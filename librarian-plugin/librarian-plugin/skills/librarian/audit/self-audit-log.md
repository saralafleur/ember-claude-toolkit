# Librarian self-audit log (append-only)

Changes to the librarian's **own** machinery — `SKILL.md`, the `librarian-*`
agents, templates, and the taxonomy itself. This is how the librarian improves
*itself*, and how we detect it thrashing on its own process (a change that reverts
a prior change). Newest at the bottom.

| Date | Target file | Change (one line) | Reverts a prior change? | Reason | Gate |
|------|-------------|-------------------|-------------------------|--------|------|
