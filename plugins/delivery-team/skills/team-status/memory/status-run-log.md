# Status Run Log (cross-project fallback, append-only)

> `status-lead` appends here **only** when the project doesn't name its own
> status-run-log in `PROJECT-CONTEXT.md`. A project-specific log (living in
> that project's own repo) is always preferred.

| Date | Project | Target | Items scanned | Overall verdict | Recommended next action |
|------|---------|--------|----------------|------------------|--------------------------|
