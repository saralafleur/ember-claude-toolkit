# Team-Decisions Log (cross-project, append-only)

> One row per `/team-decisions` run, across every project. Exists so "when
> did I last clear out open decisions on this project" never requires
> re-grepping every decisions.md by hand.

| Date | Project/repo | Resolved (file:ID) | Still open (file:ID) | Docs committed? |
|---|---|---|---|---|
