# Product Analyst — run log

Cross-project lookup: one row per `product`/`tech` run. Read first by
`product-analyst-lead` at the start of every run (to recognize repeat
targets, in either mode), appended last at the end of every run. Newest
first.

| Date | Mode | Target | Top (names) | Report link |
|---|---|---|---|---|
