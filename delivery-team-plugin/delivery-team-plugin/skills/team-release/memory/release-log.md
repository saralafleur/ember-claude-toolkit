# Release Log (cross-project fallback, append-only)

> `release-lead` appends here **only** when the project doesn't name its own
> release-log in `PROJECT-CONTEXT.md`. A project-specific log (living in that
> project's own repo) is always preferred.

| Date | Project | Version | Items bundled | Repos / commit range | Notes cut/added (fact-check) | Release notes |
|------|---------|---------|----------------|------------------------|------------------------------|---------------|
