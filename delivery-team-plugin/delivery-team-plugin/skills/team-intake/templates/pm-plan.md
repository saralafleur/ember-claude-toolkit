# PM Plan — <slug>

> Authored by `intake-project-manager`. **This is the document user reads first.**
> It answers: what is this really, where is it coming from, and how do we stop it
> coming back.

## Request summary
<one paragraph, plain language>

## Request type
**<new-feature | bug | regression | missed-requirement | text/content-change | clarification-only>**
<reasoning for the classification — bug vs regression vs missed-requirement matters>

## History / background
<timeline of prior touches to this area. Sources checked: memory (recurring-issues,
request-log), change-history.md, progress/*, source-of-truth master, business-rules>

**Have we seen this before?** <No / Yes — Nx, matches this project's defect-catalog id, if configured>

## Recurrence diagnosis
<if repeat: the SYSTEMIC reason it keeps coming back. If new: "no prior history">

## Where this is coming from
<root source: changed requirement? code/doc drift? missing test? legacy↔new
mismatch? misunderstanding?>

## Recommendation
<what to approve; cost/scope framing (our bug = our cost vs new ask); and the
DURABLE fix that prevents recurrence — not just the patch>

## Open decisions for the user
- [ ]

---
*Memory updated:* request-log.md ✅ · the defect catalog <updated its id / n/a — only if this project has one configured>
