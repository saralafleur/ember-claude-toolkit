# Request Log (cross-project fallback, append-only)

> `intake-project-manager` appends here **only** when the project doesn't
> name its own request-log in `PROJECT-CONTEXT.md`. A project-specific log
> (living in that project's own repo) is always preferred.

| Date | Project | Slug | Type | Area | One-line outcome | Link |
|------|---------|------|------|------|-------------------|------|
